/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 如果需要使用 CDN 资源，可以配置
  // images: {
  //   domains: ['your-cdn-domain.com'],
  // },
};

export default nextConfig;

